<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

    </style>
    <style name="SimpleAlertStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
        <item name="android:windowMinWidthMajor">@android:dimen/dialog_min_width_major</item>
        <item name="android:windowMinWidthMinor">@android:dimen/dialog_min_width_minor</item>
        <item name="android:windowTitleStyle">@style/SimpleAlertTitleStyle</item>
    </style>
    <style name="SimpleAlertTitleStyle" parent="SimpleAlertStyle">
        <item name="android:textAppearance">@style/SimpleAlertTitleTextStyle</item>
    </style>

    <style name="SimpleAlertTitleTextStyle" parent="SimpleAlertTitleStyle">
        <item name="android:textColor">@color/colorAccent</item>
        <!-- For some strange reason if I don't provide a text size, the text ends up being tiny -->
        <item name="android:textSize">20sp</item>
    </style>
</resources>
